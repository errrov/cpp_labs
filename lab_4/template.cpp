#include <iostream>
#include "classes.h"

template <typename T> {
    class Spec_product {
        private:
        std::string spec;
        std::size_t unit_cost;
        std::size_t size_or_allowance; // spec variable for wholesale size or allowance 

        public:
        

    }
}